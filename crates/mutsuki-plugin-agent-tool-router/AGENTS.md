# Tool Router Plugin Instructions

- Registered tools are Agent metadata over ordinary Mutsuki target protocols.
- `agent_tool` metadata does not replace protocol dispatch.
- Tool execution must either dispatch to a target protocol or return an explicit approval/error state.
