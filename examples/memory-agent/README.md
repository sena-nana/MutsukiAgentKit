# Memory Agent Example

Use `MemoryRouter::write`, `MemoryRouter::query`, and `MemoryRouter::activate` from `mutsuki-plugin-agent-memory-router` to build deterministic memory flows before adding durable storage providers.
