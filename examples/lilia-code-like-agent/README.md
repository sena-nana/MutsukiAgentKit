# Lilia-Code-Like Agent Example

This directory is intentionally documentation-only in the MVP. Lilia-specific workflows should be built as separate consumers over AgentKit protocols instead of being hard-coded into AgentKit.
