# Tool Agent Example

This example direction combines `mutsuki-plugin-agent-tool-router` with Rust crates that expose `#[agent_tool]` descriptor functions. Register each generated descriptor with `ToolRegistry::register`.
