# Agent Plugin Bundle Template

Bundle crates should expose Rust native plugins with stable protocol ids, manifests, and behavior-level tests. Keep Python runners or adapters in separate repositories.
