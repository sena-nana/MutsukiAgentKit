# Prompt

`mutsuki-plugin-agent-prompt` stores prompt templates and renders `{{variable}}` markers from typed JSON variables.

The MVP renderer is intentionally deterministic and does not execute template code.
