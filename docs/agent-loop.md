# Agent Loop

`mutsuki-plugin-agent-loop` provides `mutsuki.agent/run@1` and `mutsuki.agent.loop/step@1`.

The MVP implementation performs deterministic single-step generation and records usage and step metadata. Multi-step tool loops, approval suspension, and long-term memory orchestration are intentionally not claimed as complete in this version.
