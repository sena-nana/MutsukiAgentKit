# Memory Router

`mutsuki-plugin-agent-memory-router` provides in-memory write, query, and activate operations for Agent memory records.

The query implementation is deterministic substring and term matching. Durable vector memory belongs in a later provider plugin.
